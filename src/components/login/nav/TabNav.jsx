import { Menu } from "@mui/material";

export default function TabNav({ tab, onChangeTab }) {
  return (
    <Menu sx={{
      "& > div": {
        display: "inline-block"
      }
    }}>
        
    </Menu>
  )
}
