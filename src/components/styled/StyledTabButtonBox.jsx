import Box from '@mui/material/Box';
import { styled } from '@mui/material/styles';

const StyledTabBottonBox = styled(Box)(() => ({
  transition: "background-color 0.3s ease, box-shadow 0.3s ease"
}));

export default StyledTabBottonBox;